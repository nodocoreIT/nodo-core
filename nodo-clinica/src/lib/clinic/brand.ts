/** Full NODO | Clínica logo — same asset as verification emails (mail.ts). */
const CLINIC_BRAND_LOGO_FILE = "logo compuesto estrella az letra blanca_50.png";

export const CLINIC_BRAND_LOGO_SRC = `/logos/${encodeURI(CLINIC_BRAND_LOGO_FILE)}`;
